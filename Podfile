platform :osx, 10.11

target 'BitWake' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Starscream', '~> 2.0'

  target 'BitWakeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BitWakeUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
